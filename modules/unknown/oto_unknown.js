/**
 * @module oto
 * @category unknown
 * @label unknown
 * @position 1955 / 1990
 * @source esbuild bundle from @iflow-ai/iflow-cli v0.5.19
 * @package iflow-cli-ce (Community Edition)
 * @license Apache-2.0
 *
 * This module was automatically extracted and categorized.
 * The original variable name (oto) is preserved for compatibility.
 * DO NOT rename this module's variable - it's referenced by other modules.
 */


var oto = T(($xl, ito) => {
  "use strict";
  var Jhu = Yv(),
    Xhu = (t, e) =>
      new Jhu(t, e).set.map((r) =>
        r
          .map((n) => n.value)
          .join(" ")
          .trim()
          .split(" "),
      );
  ito.exports = Xhu;
});