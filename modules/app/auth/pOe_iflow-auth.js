/**
 * @module pOe
 * @category app/auth
 * @label iflow-auth
 * @position 117 / 1990
 * @source esbuild bundle from @iflow-ai/iflow-cli v0.5.19
 * @package iflow-cli-ce (Community Edition)
 * @license Apache-2.0
 *
 * This module was automatically extracted and categorized.
 * The original variable name (pOe) is preserved for compatibility.
 * DO NOT rename this module's variable - it's referenced by other modules.
 *
 * === Auto-analyzed semantic info ===
 * Classes: class t, class t, class extends, class t extends ste, class extends, class t extends ste
 * Functions: ite, Y5, tte, gOe, bB, ba, Mc, nte, ui, Xgo, L_t, bOe, lx, x3e, Tn
 * Features: esbuild module exports: pOe, dotenv related, MCP server handling
 * === End semantic info ===
 */


var pOe = T((Ji) => {
  "use strict";
  Object.defineProperty(Ji, "__esModule", { value: !0 });
  Ji.GoogleAuth =
    Ji.auth =
    Ji.DefaultTransporter =
    Ji.PassThroughClient =
    Ji.ExecutableError =
    Ji.PluggableAuthClient =
    Ji.DownscopedClient =
    Ji.BaseExternalAccountClient =
    Ji.ExternalAccountClient =
    Ji.IdentityPoolClient =
    Ji.AwsRequestSigner =
    Ji.AwsClient =
    Ji.UserRefreshClient =
    Ji.LoginTicket =
    Ji.ClientAuthentication =
    Ji.OAuth2Client =
    Ji.CodeChallengeMethod =
    Ji.Impersonated =
    Ji.JWT =
    Ji.JWTAccess =
    Ji.IdTokenClient =
    Ji.IAMAuth =
    Ji.GCPEnv =
    Ji.Compute =
    Ji.DEFAULT_UNIVERSE =
    Ji.AuthClient =
    Ji.gaxios =
    Ji.gcpMetadata =
      void 0;
  var fOr = cOr();
  Object.defineProperty(Ji, "GoogleAuth", {
    enumerable: !0,
    get: function () {
      return fOr.GoogleAuth;
    },
  });
  Ji.gcpMetadata = m3e();
  Ji.gaxios = xC();
  var pOr = sx();
  Object.defineProperty(Ji, "AuthClient", {
    enumerable: !0,
    get: function () {
      return pOr.AuthClient;
    },
  });
  Object.defineProperty(Ji, "DEFAULT_UNIVERSE", {
    enumerable: !0,
    get: function () {
      return pOr.DEFAULT_UNIVERSE;
    },
  });
  var bgo = gyt();
  Object.defineProperty(Ji, "Compute", {
    enumerable: !0,
    get: function () {
      return bgo.Compute;
    },
  });
  var Ago = yyt();
  Object.defineProperty(Ji, "GCPEnv", {
    enumerable: !0,
    get: function () {
      return Ago.GCPEnv;
    },
  });
  var ygo = lOr();
  Object.defineProperty(Ji, "IAMAuth", {
    enumerable: !0,
    get: function () {
      return ygo.IAMAuth;
    },
  });
  var _go = Ayt();
  Object.defineProperty(Ji, "IdTokenClient", {
    enumerable: !0,
    get: function () {
      return _go.IdTokenClient;
    },
  });
  var Ego = Pyt();
  Object.defineProperty(Ji, "JWTAccess", {
    enumerable: !0,
    get: function () {
      return Ego.JWTAccess;
    },
  });
  var vgo = Lyt();
  Object.defineProperty(Ji, "JWT", {
    enumerable: !0,
    get: function () {
      return vgo.JWT;
    },
  });
  var Cgo = $yt();
  Object.defineProperty(Ji, "Impersonated", {
    enumerable: !0,
    get: function () {
      return Cgo.Impersonated;
    },
  });
  var T_t = vG();
  Object.defineProperty(Ji, "CodeChallengeMethod", {
    enumerable: !0,
    get: function () {
      return T_t.CodeChallengeMethod;
    },
  });
  Object.defineProperty(Ji, "OAuth2Client", {
    enumerable: !0,
    get: function () {
      return T_t.OAuth2Client;
    },
  });
  Object.defineProperty(Ji, "ClientAuthentication", {
    enumerable: !0,
    get: function () {
      return T_t.ClientAuthentication;
    },
  });
  var Sgo = dyt();
  Object.defineProperty(Ji, "LoginTicket", {
    enumerable: !0,
    get: function () {
      return Sgo.LoginTicket;
    },
  });
  var wgo = Fyt();
  Object.defineProperty(Ji, "UserRefreshClient", {
    enumerable: !0,
    get: function () {
      return wgo.UserRefreshClient;
    },
  });
  var xgo = l_t();
  Object.defineProperty(Ji, "AwsClient", {
    enumerable: !0,
    get: function () {
      return xgo.AwsClient;
    },
  });
  var Tgo = s_t();
  Object.defineProperty(Ji, "AwsRequestSigner", {
    enumerable: !0,
    get: function () {
      return Tgo.AwsRequestSigner;
    },
  });
  var Dgo = i_t();
  Object.defineProperty(Ji, "IdentityPoolClient", {
    enumerable: !0,
    get: function () {
      return Dgo.IdentityPoolClient;
    },
  });
  var Igo = __t();
  Object.defineProperty(Ji, "ExternalAccountClient", {
    enumerable: !0,
    get: function () {
      return Igo.ExternalAccountClient;
    },
  });
  var Rgo = pB();
  Object.defineProperty(Ji, "BaseExternalAccountClient", {
    enumerable: !0,
    get: function () {
      return Rgo.BaseExternalAccountClient;
    },
  });
  var kgo = mOr();
  Object.defineProperty(Ji, "DownscopedClient", {
    enumerable: !0,
    get: function () {
      return kgo.DownscopedClient;
    },
  });
  var hOr = aOe();
  Object.defineProperty(Ji, "PluggableAuthClient", {
    enumerable: !0,
    get: function () {
      return hOr.PluggableAuthClient;
    },
  });
  Object.defineProperty(Ji, "ExecutableError", {
    enumerable: !0,
    get: function () {
      return hOr.ExecutableError;
    },
  });
  var Ogo = dOr();
  Object.defineProperty(Ji, "PassThroughClient", {
    enumerable: !0,
    get: function () {
      return Ogo.PassThroughClient;
    },
  });
  var Ngo = f3e();
  Object.defineProperty(Ji, "DefaultTransporter", {
    enumerable: !0,
    get: function () {
      return Ngo.DefaultTransporter;
    },
  });
  var Pgo = new fOr.GoogleAuth();
  Ji.auth = Pgo;
});
import z5, { dirname as Bgo, join as D_t } from "node:path";
import hOe from "os";
import * as gOr from "crypto";
import { fileURLToPath as Lgo } from "node:url";
import Mgo from "node:fs";
import { execSync as Fgo } from "node:child_process";
function bB() {
  return "global";
}
function ui(t = bB()) {
  return t === "aone" ? bOr : wG;
}
function Tn() {
  if (process.env.IFLOW_HOME) return z5.normalize(process.env.IFLOW_HOME);
  let t = hOe.homedir();
  return t ? z5.join(t, ui()) : z5.join(hOe.tmpdir(), ui());
}
function lx(t) {
  let e = hOe.homedir();
  return t.startsWith(e) ? t.replace(e, "~") : t;
}
function ba(t, e = 35) {
  if (t.length <= e) return t;
  let n = z5.parse(t).root,
    o = z5.sep,
    a = t
      .substring(n.length)
      .split(o)
      .filter((h) => h !== "");
  if (a.length <= 1) {
    let h = Math.floor((e - 3) / 2);
    if (h <= 0) return t.substring(0, e - 3) + "...";
    let g = t.substring(0, h),
      b = t.substring(t.length - h);
    return `${g}...${b}`;
  }
  let u = a[0],
    c = a[a.length - 1],
    m = n + u,
    d = [],
    f = o.length + c.length;
  for (let h = a.length - 2; h >= 0; h--) {
    let g = a[h],
      b = f + o.length + g.length;
    if (b <= e) (d.unshift(g), (f = b));
    else break;
  }
  let p = d.join(o) + o + c;
  return f > e ? p : ((p = m + o + p), p.length > e ? "..." + p.substring(p.length - e - 3) : p);
}
function Mc(t, e) {
  let r = z5.resolve(t),
    n = z5.resolve(e);
  return z5.relative(n, r) || ".";
}
function tte(t) {
  let e = "";
  for (let r = 0; r < t.length; r++) t[r] === " " && (r === 0 || t[r - 1] !== "\\") ? (e += "\\ ") : (e += t[r]);
  return e;
}
function hR(t) {
  return process.platform === "win32" ? t.replace(/\\([ ,;!?()[\]{}])/g, "$1") : t.replace(/\\(.)/g, "$1");
}
function gOe(t) {
  return gOr.createHash("sha256").update(t).digest("hex");
}
function Y5(t) {
  let e = gOe(t);
  return z5.join(Tn(), Ugo, e);
}
function rte() {
  return z5.join(Tn(), AOr);
}
function nte(t) {
  return z5.join(t, ui(), AOr);
}
function bOe() {
  if (process.env.USE_BUILTIN_RIPGREP === "0") return "rg";
  let t = Bgo(Lgo(import.meta.url)),
    e = [
      D_t(
        t,
        "..",
        "vendors",
        "ripgrep",
        `${process.arch}-${process.platform}`,
        `rg${process.platform === "win32" ? ".exe" : ""}`,
      ),
      D_t(
        t,
        "..",
        "..",
        "..",
        "..",
        "vendors",
        "ripgrep",
        `${process.arch}-${process.platform}`,
        `rg${process.platform === "win32" ? ".exe" : ""}`,
      ),
      D_t(
        t,
        "..",
        "..",
        "..",
        "vendors",
        "ripgrep",
        `${process.arch}-${process.platform}`,
        `rg${process.platform === "win32" ? ".exe" : ""}`,
      ),
    ];
  for (let r of e)
    try {
      if (Mgo.existsSync(r)) return (process.platform === "darwin" && yOr(r), r);
    } catch {}
  return e[0];
}
function yOr(t) {
  if (process.platform === "darwin")
    try {
      let e = ["com.apple.quarantine", "CachedFileMimeType", "CachedFileType", "FileXRayCachedResultInEA"];
      for (let r of e)
        try {
          Fgo(`xattr -d "${r}" "${t}" 2>/dev/null`, { stdio: "ignore" });
        } catch {}
    } catch {}
}
function ite(t, e) {
  let n = hOe.platform() === "win32" ? z5.win32 : z5,
    o = n.relative(t, e);
  return !o.startsWith(`..${n.sep}`) && o !== ".." && !n.isAbsolute(o);
}
function x3e(t) {
  let r = z5
    .resolve(t)
    .replace(/\\/g, "-")
    .replace(/\//g, "-")
    .replace(/:/g, "-")
    .replace(/\s+/g, "-")
    .replace(/[^\p{L}\p{N}\-_.]/gu, "-");
  return ((r = r.replace(/-+/g, "-")), (r = r.replace(/^-+|-+$/g, "")), r ? "-" + r : "-root");
}
var wG,
  bOr,
  I_t,
  Ugo,
  AOr,
  w3e,
  Pa = j(() => {
    "use strict";
    ((wG = ".iflow"), (bOr = ".iflow-aone"), (I_t = "google_accounts.json"), (Ugo = "tmp"), (AOr = "commands"));
    w3e = ui();
  });
import * as Sp from "node:path";
import * as _Or from "crypto";
import * as EOr from "fs";
var $go,
  jgo,
  wp,
  T3e = j(() => {
    "use strict";
    Pa();
    (($go = "iflow_accounts.json"),
      (jgo = "tmp"),
      (wp = class t {
        targetDir;
        constructor(e) {
          this.targetDir = e;
        }
        static getGlobalIFlowDir() {
          return Tn();
        }
        static getMcpOAuthTokensPath() {
          return Sp.join(t.getGlobalIFlowDir(), "mcp-oauth-tokens.json");
        }
        static getGlobalSettingsPath() {
          return Sp.join(t.getGlobalIFlowDir(), "settings.json");
        }
        static getInstallationIdPath() {
          return Sp.join(t.getGlobalIFlowDir(), "installation_id");
        }
        static getIFlowAccountsPath() {
          return Sp.join(t.getGlobalIFlowDir(), $go);
        }
        static getUserCommandsDir() {
          return Sp.join(t.getGlobalIFlowDir(), "commands");
        }
        static getGlobalMemoryFilePath() {
          return Sp.join(t.getGlobalIFlowDir(), "memory.md");
        }
        static getGlobalTempDir() {
          return Sp.join(t.getGlobalIFlowDir(), jgo);
        }
        getIFlowDir() {
          return Sp.join(this.targetDir, ui());
        }
        getProjectTempDir() {
          let e = this.getFilePathHash(this.getProjectRoot()),
            r = t.getGlobalTempDir();
          return Sp.join(r, e);
        }
        ensureProjectTempDirExists() {
          EOr.mkdirSync(this.getProjectTempDir(), { recursive: !0 });
        }
        static getOAuthCredsPath() {
          return Sp.join(t.getGlobalIFlowDir(), "oauth_creds.json");
        }
        getProjectRoot() {
          return this.targetDir;
        }
        getFilePathHash(e) {
          return _Or.createHash("sha256").update(e).digest("hex");
        }
        getHistoryDir() {
          let e = this.getFilePathHash(this.getProjectRoot()),
            r = Sp.join(t.getGlobalIFlowDir(), "history");
          return Sp.join(r, e);
        }
        getWorkspaceSettingsPath() {
          return Sp.join(this.getIFlowDir(), "settings.json");
        }
        getProjectCommandsDir() {
          return Sp.join(this.getIFlowDir(), "commands");
        }
        getProjectTempCheckpointsDir() {
          return Sp.join(this.getProjectTempDir(), "checkpoints");
        }
        getExtensionsDir() {
          return Sp.join(this.getIFlowDir(), "extensions");
        }
        getExtensionsConfigPath() {
          return Sp.join(this.getExtensionsDir(), "iflow-extension.json");
        }
        getHistoryFilePath() {
          return Sp.join(this.getProjectTempDir(), "shell_history");
        }
      }));
  });
function Xgo() {
  let t = [],
    e = Object.create(null),
    r;
  return (
    (e.get = (n, o) => (r?.revoke?.(), o === POr ? t : (t.push(o), (r = Proxy.revocable(n, e)), r.proxy))),
    Proxy.revocable(Object.create(null), e).proxy
  );
}
function L_t(t, e) {
  let { [POr]: r } = t(Xgo());
  return r.join(e?.keySeparator ?? ".");
}
var ls,
  D3e,
  vOr,
  Qgo,
  Ggo,
  COr,
  SOr,
  I3e,
  wOr,
  qgo,
  yOe,
  Hgo,
  OOr,
  ote,
  Vgo,
  Wgo,
  N_t,
  zgo,
  Ygo,
  Kgo,
  P_t,
  R3e,
  Jgo,
  B_t,
  mx,
  ste,
  _Oe,
  NOr,
  POr,
  xOr,
  R_t,
  EOe,
  vOe,
  TOr,
  DOr,
  M_t,
  IOr,
  k_t,
  COe,
  Zgo,
  ROr,
  ebo,
  F_t,
  tbo,
  U_t,
  O_t,
  kOr,
  AOe,
  rbo,
  $_t,
  I,
  BSu,
  LSu,
  MSu,
  FSu,
  USu,
  $Su,
  jSu,
  QSu,
  GSu,
  qSu,
  HSu,
  VSu,
  WSu,
  zSu,
  BOr = j(() => {
    ((ls = (t) => typeof t == "string"),
      (D3e = () => {
        let t,
          e,
          r = new Promise((n, o) => {
            ((t = n), (e = o));
          });
        return ((r.resolve = t), (r.reject = e), r);
      }),
      (vOr = (t) => (t == null ? "" : "" + t)),
      (Qgo = (t, e, r) => {
        t.forEach((n) => {
          e[n] && (r[n] = e[n]);
        });
      }),
      (Ggo = /###/g),
      (COr = (t) => (t && t.indexOf("###") > -1 ? t.replace(Ggo, ".") : t)),
      (SOr = (t) => !t || ls(t)),
      (I3e = (t, e, r) => {
        let n = ls(e) ? e.split(".") : e,
          o = 0;
        for (; o < n.length - 1; ) {
          if (SOr(t)) return {};
          let s = COr(n[o]);
          (!t[s] && r && (t[s] = new r()), Object.prototype.hasOwnProperty.call(t, s) ? (t = t[s]) : (t = {}), ++o);
        }
        return SOr(t) ? {} : { obj: t, k: COr(n[o]) };
      }),
      (wOr = (t, e, r) => {
        let { obj: n, k: o } = I3e(t, e, Object);
        if (n !== void 0 || e.length === 1) {
          n[o] = r;
          return;
        }
        let s = e[e.length - 1],
          a = e.slice(0, e.length - 1),
          u = I3e(t, a, Object);
        for (; u.obj === void 0 && a.length; )
          ((s = `${a[a.length - 1]}.${s}`),
            (a = a.slice(0, a.length - 1)),
            (u = I3e(t, a, Object)),
            u?.obj && typeof u.obj[`${u.k}.${s}`] < "u" && (u.obj = void 0));
        u.obj[`${u.k}.${s}`] = r;
      }),
      (qgo = (t, e, r, n) => {
        let { obj: o, k: s } = I3e(t, e, Object);
        ((o[s] = o[s] || []), o[s].push(r));
      }),
      (yOe = (t, e) => {
        let { obj: r, k: n } = I3e(t, e);
        if (r && Object.prototype.hasOwnProperty.call(r, n)) return r[n];
      }),
      (Hgo = (t, e, r) => {
        let n = yOe(t, r);
        return n !== void 0 ? n : yOe(e, r);
      }),
      (OOr = (t, e, r) => {
        for (let n in e)
          n !== "__proto__" &&
            n !== "constructor" &&
            (n in t
              ? ls(t[n]) || t[n] instanceof String || ls(e[n]) || e[n] instanceof String
                ? r && (t[n] = e[n])
                : OOr(t[n], e[n], r)
              : (t[n] = e[n]));
        return t;
      }),
      (ote = (t) => t.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&")),
      (Vgo = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;", "/": "&#x2F;" }),
      (Wgo = (t) => (ls(t) ? t.replace(/[&<>"'\/]/g, (e) => Vgo[e]) : t)),
      (N_t = class {
        constructor(e) {
          ((this.capacity = e), (this.regExpMap = new Map()), (this.regExpQueue = []));
        }
        getRegExp(e) {
          let r = this.regExpMap.get(e);
          if (r !== void 0) return r;
          let n = new RegExp(e);
          return (
            this.regExpQueue.length === this.capacity && this.regExpMap.delete(this.regExpQueue.shift()),
            this.regExpMap.set(e, n),
            this.regExpQueue.push(e),
            n
          );
        }
      }),
      (zgo = [" ", ",", "?", "!", ";"]),
      (Ygo = new N_t(20)),
      (Kgo = (t, e, r) => {
        ((e = e || ""), (r = r || ""));
        let n = zgo.filter((a) => e.indexOf(a) < 0 && r.indexOf(a) < 0);
        if (n.length === 0) return !0;
        let o = Ygo.getRegExp(`(${n.map((a) => (a === "?" ? "\\?" : a)).join("|")})`),
          s = !o.test(t);
        if (!s) {
          let a = t.indexOf(r);
          a > 0 && !o.test(t.substring(0, a)) && (s = !0);
        }
        return s;
      }),
      (P_t = (t, e, r = ".") => {
        if (!t) return;
        if (t[e]) return Object.prototype.hasOwnProperty.call(t, e) ? t[e] : void 0;
        let n = e.split(r),
          o = t;
        for (let s = 0; s < n.length; ) {
          if (!o || typeof o != "object") return;
          let a,
            u = "";
          for (let c = s; c < n.length; ++c)
            if ((c !== s && (u += r), (u += n[c]), (a = o[u]), a !== void 0)) {
              if (["string", "number", "boolean"].indexOf(typeof a) > -1 && c < n.length - 1) continue;
              s += c - s + 1;
              break;
            }
          o = a;
        }
        return o;
      }),
      (R3e = (t) => t?.replace("_", "-")),
      (Jgo = {
        type: "logger",
        log(t) {
          this.output("log", t);
        },
        warn(t) {
          this.output("warn", t);
        },
        error(t) {
          this.output("error", t);
        },
        output(t, e) {
          console?.[t]?.apply?.(console, e);
        },
      }),
      (B_t = class t {
        constructor(e, r = {}) {
          this.init(e, r);
        }
        init(e, r = {}) {
          ((this.prefix = r.prefix || "i18next:"),
            (this.logger = e || Jgo),
            (this.options = r),
            (this.debug = r.debug));
        }
        log(...e) {
          return this.forward(e, "log", "", !0);
        }
        warn(...e) {
          return this.forward(e, "warn", "", !0);
        }
        error(...e) {
          return this.forward(e, "error", "");
        }
        deprecate(...e) {
          return this.forward(e, "warn", "WARNING DEPRECATED: ", !0);
        }
        forward(e, r, n, o) {
          return o && !this.debug ? null : (ls(e[0]) && (e[0] = `${n}${this.prefix} ${e[0]}`), this.logger[r](e));
        }
        create(e) {
          return new t(this.logger, { prefix: `${this.prefix}:${e}:`, ...this.options });
        }
        clone(e) {
          return ((e = e || this.options), (e.prefix = e.prefix || this.prefix), new t(this.logger, e));
        }
      }),
      (mx = new B_t()),
      (ste = class {
        constructor() {
          this.observers = {};
        }
        on(e, r) {
          return (
            e.split(" ").forEach((n) => {
              this.observers[n] || (this.observers[n] = new Map());
              let o = this.observers[n].get(r) || 0;
              this.observers[n].set(r, o + 1);
            }),
            this
          );
        }
        off(e, r) {
          if (this.observers[e]) {
            if (!r) {
              delete this.observers[e];
              return;
            }
            this.observers[e].delete(r);
          }
        }
        emit(e, ...r) {
          (this.observers[e] &&
            Array.from(this.observers[e].entries()).forEach(([o, s]) => {
              for (let a = 0; a < s; a++) o(...r);
            }),
            this.observers["*"] &&
              Array.from(this.observers["*"].entries()).forEach(([o, s]) => {
                for (let a = 0; a < s; a++) o.apply(o, [e, ...r]);
              }));
        }
      }),
      (_Oe = class extends ste {
        constructor(e, r = { ns: ["translation"], defaultNS: "translation" }) {
          (super(),
            (this.data = e || {}),
            (this.options = r),
            this.options.keySeparator === void 0 && (this.options.keySeparator = "."),
            this.options.ignoreJSONStructure === void 0 && (this.options.ignoreJSONStructure = !0));
        }
        addNamespaces(e) {
          this.options.ns.indexOf(e) < 0 && this.options.ns.push(e);
        }
        removeNamespaces(e) {
          let r = this.options.ns.indexOf(e);
          r > -1 && this.options.ns.splice(r, 1);
        }
        getResource(e, r, n, o = {}) {
          let s = o.keySeparator !== void 0 ? o.keySeparator : this.options.keySeparator,
            a = o.ignoreJSONStructure !== void 0 ? o.ignoreJSONStructure : this.options.ignoreJSONStructure,
            u;
          e.indexOf(".") > -1
            ? (u = e.split("."))
            : ((u = [e, r]), n && (Array.isArray(n) ? u.push(...n) : ls(n) && s ? u.push(...n.split(s)) : u.push(n)));
          let c = yOe(this.data, u);
          return (
            !c && !r && !n && e.indexOf(".") > -1 && ((e = u[0]), (r = u[1]), (n = u.slice(2).join("."))),
            c || !a || !ls(n) ? c : P_t(this.data?.[e]?.[r], n, s)
          );
        }
        addResource(e, r, n, o, s = { silent: !1 }) {
          let a = s.keySeparator !== void 0 ? s.keySeparator : this.options.keySeparator,
            u = [e, r];
          (n && (u = u.concat(a ? n.split(a) : n)),
            e.indexOf(".") > -1 && ((u = e.split(".")), (o = r), (r = u[1])),
            this.addNamespaces(r),
            wOr(this.data, u, o),
            s.silent || this.emit("added", e, r, n, o));
        }
        addResources(e, r, n, o = { silent: !1 }) {
          for (let s in n) (ls(n[s]) || Array.isArray(n[s])) && this.addResource(e, r, s, n[s], { silent: !0 });
          o.silent || this.emit("added", e, r, n);
        }
        addResourceBundle(e, r, n, o, s, a = { silent: !1, skipCopy: !1 }) {
          let u = [e, r];
          (e.indexOf(".") > -1 && ((u = e.split(".")), (o = n), (n = r), (r = u[1])), this.addNamespaces(r));
          let c = yOe(this.data, u) || {};
          (a.skipCopy || (n = JSON.parse(JSON.stringify(n))),
            o ? OOr(c, n, s) : (c = { ...c, ...n }),
            wOr(this.data, u, c),
            a.silent || this.emit("added", e, r, n));
        }
        removeResourceBundle(e, r) {
          (this.hasResourceBundle(e, r) && delete this.data[e][r],
            this.removeNamespaces(r),
            this.emit("removed", e, r));
        }
        hasResourceBundle(e, r) {
          return this.getResource(e, r) !== void 0;
        }
        getResourceBundle(e, r) {
          return (r || (r = this.options.defaultNS), this.getResource(e, r));
        }
        getDataByLanguage(e) {
          return this.data[e];
        }
        hasLanguageSomeTranslations(e) {
          let r = this.getDataByLanguage(e);
          return !!((r && Object.keys(r)) || []).find((o) => r[o] && Object.keys(r[o]).length > 0);
        }
        toJSON() {
          return this.data;
        }
      }),
      (NOr = {
        processors: {},
        addPostProcessor(t) {
          this.processors[t.name] = t;
        },
        handle(t, e, r, n, o) {
          return (
            t.forEach((s) => {
              e = this.processors[s]?.process(e, r, n, o) ?? e;
            }),
            e
          );
        },
      }),
      (POr = Symbol("i18next/PATH_KEY")));
    ((xOr = {}),
      (R_t = (t) => !ls(t) && typeof t != "boolean" && typeof t != "number"),
      (EOe = class t extends ste {
        constructor(e, r = {}) {
          (super(),
            Qgo(
              [
                "resourceStore",
                "languageUtils",
                "pluralResolver",
                "interpolator",
                "backendConnector",
                "i18nFormat",
                "utils",
              ],
              e,
              this,
            ),
            (this.options = r),
            this.options.keySeparator === void 0 && (this.options.keySeparator = "."),
            (this.logger = mx.create("translator")));
        }
        changeLanguage(e) {
          e && (this.language = e);
        }
        exists(e, r = { interpolation: {} }) {
          let n = { ...r };
          if (e == null) return !1;
          let o = this.resolve(e, n);
          if (o?.res === void 0) return !1;
          let s = R_t(o.res);
          return !(n.returnObjects === !1 && s);
        }
        extractFromKey(e, r) {
          let n = r.nsSeparator !== void 0 ? r.nsSeparator : this.options.nsSeparator;
          n === void 0 && (n = ":");
          let o = r.keySeparator !== void 0 ? r.keySeparator : this.options.keySeparator,
            s = r.ns || this.options.defaultNS || [],
            a = n && e.indexOf(n) > -1,
            u =
              !this.options.userDefinedKeySeparator &&
              !r.keySeparator &&
              !this.options.userDefinedNsSeparator &&
              !r.nsSeparator &&
              !Kgo(e, n, o);
          if (a && !u) {
            let c = e.match(this.interpolator.nestingRegexp);
            if (c && c.length > 0) return { key: e, namespaces: ls(s) ? [s] : s };
            let m = e.split(n);
            ((n !== o || (n === o && this.options.ns.indexOf(m[0]) > -1)) && (s = m.shift()), (e = m.join(o)));
          }
          return { key: e, namespaces: ls(s) ? [s] : s };
        }
        translate(e, r, n) {
          let o = typeof r == "object" ? { ...r } : r;
          if (
            (typeof o != "object" &&
              this.options.overloadTranslationOptionHandler &&
              (o = this.options.overloadTranslationOptionHandler(arguments)),
            typeof o == "object" && (o = { ...o }),
            o || (o = {}),
            e == null)
          )
            return "";
          (typeof e == "function" && (e = L_t(e, { ...this.options, ...o })), Array.isArray(e) || (e = [String(e)]));
          let s = o.returnDetails !== void 0 ? o.returnDetails : this.options.returnDetails,
            a = o.keySeparator !== void 0 ? o.keySeparator : this.options.keySeparator,
            { key: u, namespaces: c } = this.extractFromKey(e[e.length - 1], o),
            m = c[c.length - 1],
            d = o.nsSeparator !== void 0 ? o.nsSeparator : this.options.nsSeparator;
          d === void 0 && (d = ":");
          let f = o.lng || this.language,
            p = o.appendNamespaceToCIMode || this.options.appendNamespaceToCIMode;
          if (f?.toLowerCase() === "cimode")
            return p
              ? s
                ? {
                    res: `${m}${d}${u}`,
                    usedKey: u,
                    exactUsedKey: u,
                    usedLng: f,
                    usedNS: m,
                    usedParams: this.getUsedParamsDetails(o),
                  }
                : `${m}${d}${u}`
              : s
                ? {
                    res: u,
                    usedKey: u,
                    exactUsedKey: u,
                    usedLng: f,
                    usedNS: m,
                    usedParams: this.getUsedParamsDetails(o),
                  }
                : u;
          let h = this.resolve(e, o),
            g = h?.res,
            b = h?.usedKey || u,
            A = h?.exactUsedKey || u,
            y = ["[object Number]", "[object Function]", "[object RegExp]"],
            E = o.joinArrays !== void 0 ? o.joinArrays : this.options.joinArrays,
            v = !this.i18nFormat || this.i18nFormat.handleAsObject,
            C = o.count !== void 0 && !ls(o.count),
            x = t.hasDefaultValue(o),
            k = C ? this.pluralResolver.getSuffix(f, o.count, o) : "",
            R = o.ordinal && C ? this.pluralResolver.getSuffix(f, o.count, { ordinal: !1 }) : "",
            P = C && !o.ordinal && o.count === 0,
            D =
              (P && o[`defaultValue${this.options.pluralSeparator}zero`]) ||
              o[`defaultValue${k}`] ||
              o[`defaultValue${R}`] ||
              o.defaultValue,
            O = g;
          v && !g && x && (O = D);
          let N = R_t(O),
            F = Object.prototype.toString.apply(O);
          if (v && O && N && y.indexOf(F) < 0 && !(ls(E) && Array.isArray(O))) {
            if (!o.returnObjects && !this.options.returnObjects) {
              this.options.returnedObjectHandler ||
                this.logger.warn("accessing an object - but returnObjects options is not enabled!");
              let B = this.options.returnedObjectHandler
                ? this.options.returnedObjectHandler(b, O, { ...o, ns: c })
                : `key '${u} (${this.language})' returned an object instead of string.`;
              return s ? ((h.res = B), (h.usedParams = this.getUsedParamsDetails(o)), h) : B;
            }
            if (a) {
              let B = Array.isArray(O),
                L = B ? [] : {},
                G = B ? A : b;
              for (let Q in O)
                if (Object.prototype.hasOwnProperty.call(O, Q)) {
                  let K = `${G}${a}${Q}`;
                  (x && !g
                    ? (L[Q] = this.translate(K, { ...o, defaultValue: R_t(D) ? D[Q] : void 0, joinArrays: !1, ns: c }))
                    : (L[Q] = this.translate(K, { ...o, joinArrays: !1, ns: c })),
                    L[Q] === K && (L[Q] = O[Q]));
                }
              g = L;
            }
          } else if (v && ls(E) && Array.isArray(g)) ((g = g.join(E)), g && (g = this.extendTranslation(g, e, o, n)));
          else {
            let B = !1,
              L = !1;
            (!this.isValidLookup(g) && x && ((B = !0), (g = D)), this.isValidLookup(g) || ((L = !0), (g = u)));
            let Q = (o.missingKeyNoValueFallbackToKey || this.options.missingKeyNoValueFallbackToKey) && L ? void 0 : g,
              K = x && D !== g && this.options.updateMissing;
            if (L || B || K) {
              if ((this.logger.log(K ? "updateKey" : "missingKey", f, m, u, K ? D : g), a)) {
                let X = this.resolve(u, { ...o, keySeparator: !1 });
                X &&
                  X.res &&
                  this.logger.warn(
                    "Seems the loaded translations were in flat JSON format instead of nested. Either set keySeparator: false on init or make sure your translations are published in nested format.",
                  );
              }
              let H = [],
                U = this.languageUtils.getFallbackCodes(this.options.fallbackLng, o.lng || this.language);
              if (this.options.saveMissingTo === "fallback" && U && U[0])
                for (let X = 0; X < U.length; X++) H.push(U[X]);
              else
                this.options.saveMissingTo === "all"
                  ? (H = this.languageUtils.toResolveHierarchy(o.lng || this.language))
                  : H.push(o.lng || this.language);
              let Y = (X, J, q) => {
                let ne = x && q !== g ? q : Q;
                (this.options.missingKeyHandler
                  ? this.options.missingKeyHandler(X, m, J, ne, K, o)
                  : this.backendConnector?.saveMissing && this.backendConnector.saveMissing(X, m, J, ne, K, o),
                  this.emit("missingKey", X, m, J, g));
              };
              this.options.saveMissing &&
                (this.options.saveMissingPlurals && C
                  ? H.forEach((X) => {
                      let J = this.pluralResolver.getSuffixes(X, o);
                      (P &&
                        o[`defaultValue${this.options.pluralSeparator}zero`] &&
                        J.indexOf(`${this.options.pluralSeparator}zero`) < 0 &&
                        J.push(`${this.options.pluralSeparator}zero`),
                        J.forEach((q) => {
                          Y([X], u + q, o[`defaultValue${q}`] || D);
                        }));
                    })
                  : Y(H, u, D));
            }
            ((g = this.extendTranslation(g, e, o, h, n)),
              L && g === u && this.options.appendNamespaceToMissingKey && (g = `${m}${d}${u}`),
              (L || B) &&
                this.options.parseMissingKeyHandler &&
                (g = this.options.parseMissingKeyHandler(
                  this.options.appendNamespaceToMissingKey ? `${m}${d}${u}` : u,
                  B ? g : void 0,
                  o,
                )));
          }
          return s ? ((h.res = g), (h.usedParams = this.getUsedParamsDetails(o)), h) : g;
        }
        extendTranslation(e, r, n, o, s) {
          if (this.i18nFormat?.parse)
            e = this.i18nFormat.parse(
              e,
              { ...this.options.interpolation.defaultVariables, ...n },
              n.lng || this.language || o.usedLng,
              o.usedNS,
              o.usedKey,
              { resolved: o },
            );
          else if (!n.skipInterpolation) {
            n.interpolation &&
              this.interpolator.init({ ...n, interpolation: { ...this.options.interpolation, ...n.interpolation } });
            let c =
                ls(e) &&
                (n?.interpolation?.skipOnVariables !== void 0
                  ? n.interpolation.skipOnVariables
                  : this.options.interpolation.skipOnVariables),
              m;
            if (c) {
              let f = e.match(this.interpolator.nestingRegexp);
              m = f && f.length;
            }
            let d = n.replace && !ls(n.replace) ? n.replace : n;
            if (
              (this.options.interpolation.defaultVariables &&
                (d = { ...this.options.interpolation.defaultVariables, ...d }),
              (e = this.interpolator.interpolate(e, d, n.lng || this.language || o.usedLng, n)),
              c)
            ) {
              let f = e.match(this.interpolator.nestingRegexp),
                p = f && f.length;
              m < p && (n.nest = !1);
            }
            (!n.lng && o && o.res && (n.lng = this.language || o.usedLng),
              n.nest !== !1 &&
                (e = this.interpolator.nest(
                  e,
                  (...f) =>
                    s?.[0] === f[0] && !n.context
                      ? (this.logger.warn(`It seems you are nesting recursively key: ${f[0]} in key: ${r[0]}`), null)
                      : this.translate(...f, r),
                  n,
                )),
              n.interpolation && this.interpolator.reset());
          }
          let a = n.postProcess || this.options.postProcess,
            u = ls(a) ? [a] : a;
          return (
            e != null &&
              u?.length &&
              n.applyPostProcessor !== !1 &&
              (e = NOr.handle(
                u,
                e,
                r,
                this.options && this.options.postProcessPassResolved
                  ? { i18nResolved: { ...o, usedParams: this.getUsedParamsDetails(n) }, ...n }
                  : n,
                this,
              )),
            e
          );
        }
        resolve(e, r = {}) {
          let n, o, s, a, u;
          return (
            ls(e) && (e = [e]),
            e.forEach((c) => {
              if (this.isValidLookup(n)) return;
              let m = this.extractFromKey(c, r),
                d = m.key;
              o = d;
              let f = m.namespaces;
              this.options.fallbackNS && (f = f.concat(this.options.fallbackNS));
              let p = r.count !== void 0 && !ls(r.count),
                h = p && !r.ordinal && r.count === 0,
                g = r.context !== void 0 && (ls(r.context) || typeof r.context == "number") && r.context !== "",
                b = r.lngs ? r.lngs : this.languageUtils.toResolveHierarchy(r.lng || this.language, r.fallbackLng);
              f.forEach((A) => {
                this.isValidLookup(n) ||
                  ((u = A),
                  !xOr[`${b[0]}-${A}`] &&
                    this.utils?.hasLoadedNamespace &&
                    !this.utils?.hasLoadedNamespace(u) &&
                    ((xOr[`${b[0]}-${A}`] = !0),
                    this.logger.warn(
                      `key "${o}" for languages "${b.join(", ")}" won't get resolved as namespace "${u}" was not yet loaded`,
                      "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!",
                    )),
                  b.forEach((y) => {
                    if (this.isValidLookup(n)) return;
                    a = y;
                    let E = [d];
                    if (this.i18nFormat?.addLookupKeys) this.i18nFormat.addLookupKeys(E, d, y, A, r);
                    else {
                      let C;
                      p && (C = this.pluralResolver.getSuffix(y, r.count, r));
                      let x = `${this.options.pluralSeparator}zero`,
                        k = `${this.options.pluralSeparator}ordinal${this.options.pluralSeparator}`;
                      if (
                        (p &&
                          (r.ordinal && C.indexOf(k) === 0 && E.push(d + C.replace(k, this.options.pluralSeparator)),
                          E.push(d + C),
                          h && E.push(d + x)),
                        g)
                      ) {
                        let R = `${d}${this.options.contextSeparator || "_"}${r.context}`;
                        (E.push(R),
                          p &&
                            (r.ordinal && C.indexOf(k) === 0 && E.push(R + C.replace(k, this.options.pluralSeparator)),
                            E.push(R + C),
                            h && E.push(R + x)));
                      }
                    }
                    let v;
                    for (; (v = E.pop()); ) this.isValidLookup(n) || ((s = v), (n = this.getResource(y, A, v, r)));
                  }));
              });
            }),
            { res: n, usedKey: o, exactUsedKey: s, usedLng: a, usedNS: u }
          );
        }
        isValidLookup(e) {
          return (
            e !== void 0 && !(!this.options.returnNull && e === null) && !(!this.options.returnEmptyString && e === "")
          );
        }
        getResource(e, r, n, o = {}) {
          return this.i18nFormat?.getResource
            ? this.i18nFormat.getResource(e, r, n, o)
            : this.resourceStore.getResource(e, r, n, o);
        }
        getUsedParamsDetails(e = {}) {
          let r = [
              "defaultValue",
              "ordinal",
              "context",
              "replace",
              "lng",
              "lngs",
              "fallbackLng",
              "ns",
              "keySeparator",
              "nsSeparator",
              "returnObjects",
              "returnDetails",
              "joinArrays",
              "postProcess",
              "interpolation",
            ],
            n = e.replace && !ls(e.replace),
            o = n ? e.replace : e;
          if (
            (n && typeof e.count < "u" && (o.count = e.count),
            this.options.interpolation.defaultVariables &&
              (o = { ...this.options.interpolation.defaultVariables, ...o }),
            !n)
          ) {
            o = { ...o };
            for (let s of r) delete o[s];
          }
          return o;
        }
        static hasDefaultValue(e) {
          let r = "defaultValue";
          for (let n in e)
            if (Object.prototype.hasOwnProperty.call(e, n) && r === n.substring(0, r.length) && e[n] !== void 0)
              return !0;
          return !1;
        }
      }),
      (vOe = class {
        constructor(e) {
          ((this.options = e),
            (this.supportedLngs = this.options.supportedLngs || !1),
            (this.logger = mx.create("languageUtils")));
        }
        getScriptPartFromCode(e) {
          if (((e = R3e(e)), !e || e.indexOf("-") < 0)) return null;
          let r = e.split("-");
          return r.length === 2 || (r.pop(), r[r.length - 1].toLowerCase() === "x")
            ? null
            : this.formatLanguageCode(r.join("-"));
        }
        getLanguagePartFromCode(e) {
          if (((e = R3e(e)), !e || e.indexOf("-") < 0)) return e;
          let r = e.split("-");
          return this.formatLanguageCode(r[0]);
        }
        formatLanguageCode(e) {
          if (ls(e) && e.indexOf("-") > -1) {
            let r;
            try {
              r = Intl.getCanonicalLocales(e)[0];
            } catch {}
            return (
              r && this.options.lowerCaseLng && (r = r.toLowerCase()),
              r || (this.options.lowerCaseLng ? e.toLowerCase() : e)
            );
          }
          return this.options.cleanCode || this.options.lowerCaseLng ? e.toLowerCase() : e;
        }
        isSupportedCode(e) {
          return (
            (this.options.load === "languageOnly" || this.options.nonExplicitSupportedLngs) &&
              (e = this.getLanguagePartFromCode(e)),
            !this.supportedLngs || !this.supportedLngs.length || this.supportedLngs.indexOf(e) > -1
          );
        }
        getBestMatchFromCodes(e) {
          if (!e) return null;
          let r;
          return (
            e.forEach((n) => {
              if (r) return;
              let o = this.formatLanguageCode(n);
              (!this.options.supportedLngs || this.isSupportedCode(o)) && (r = o);
            }),
            !r &&
              this.options.supportedLngs &&
              e.forEach((n) => {
                if (r) return;
                let o = this.getScriptPartFromCode(n);
                if (this.isSupportedCode(o)) return (r = o);
                let s = this.getLanguagePartFromCode(n);
                if (this.isSupportedCode(s)) return (r = s);
                r = this.options.supportedLngs.find((a) => {
                  if (a === s) return a;
                  if (
                    !(a.indexOf("-") < 0 && s.indexOf("-") < 0) &&
                    ((a.indexOf("-") > 0 && s.indexOf("-") < 0 && a.substring(0, a.indexOf("-")) === s) ||
                      (a.indexOf(s) === 0 && s.length > 1))
                  )
                    return a;
                });
              }),
            r || (r = this.getFallbackCodes(this.options.fallbackLng)[0]),
            r
          );
        }
        getFallbackCodes(e, r) {
          if (!e) return [];
          if ((typeof e == "function" && (e = e(r)), ls(e) && (e = [e]), Array.isArray(e))) return e;
          if (!r) return e.default || [];
          let n = e[r];
          return (
            n || (n = e[this.getScriptPartFromCode(r)]),
            n || (n = e[this.formatLanguageCode(r)]),
            n || (n = e[this.getLanguagePartFromCode(r)]),
            n || (n = e.default),
            n || []
          );
        }
        toResolveHierarchy(e, r) {
          let n = this.getFallbackCodes((r === !1 ? [] : r) || this.options.fallbackLng || [], e),
            o = [],
            s = (a) => {
              a &&
                (this.isSupportedCode(a)
                  ? o.push(a)
                  : this.logger.warn(`rejecting language code not found in supportedLngs: ${a}`));
            };
          return (
            ls(e) && (e.indexOf("-") > -1 || e.indexOf("_") > -1)
              ? (this.options.load !== "languageOnly" && s(this.formatLanguageCode(e)),
                this.options.load !== "languageOnly" &&
                  this.options.load !== "currentOnly" &&
                  s(this.getScriptPartFromCode(e)),
                this.options.load !== "currentOnly" && s(this.getLanguagePartFromCode(e)))
              : ls(e) && s(this.formatLanguageCode(e)),
            n.forEach((a) => {
              o.indexOf(a) < 0 && s(this.formatLanguageCode(a));
            }),
            o
          );
        }
      }),
      (TOr = { zero: 0, one: 1, two: 2, few: 3, many: 4, other: 5 }),
      (DOr = {
        select: (t) => (t === 1 ? "one" : "other"),
        resolvedOptions: () => ({ pluralCategories: ["one", "other"] }),
      }),
      (M_t = class {
        constructor(e, r = {}) {
          ((this.languageUtils = e),
            (this.options = r),
            (this.logger = mx.create("pluralResolver")),
            (this.pluralRulesCache = {}));
        }
        clearCache() {
          this.pluralRulesCache = {};
        }
        getRule(e, r = {}) {
          let n = R3e(e === "dev" ? "en" : e),
            o = r.ordinal ? "ordinal" : "cardinal",
            s = JSON.stringify({ cleanedCode: n, type: o });
          if (s in this.pluralRulesCache) return this.pluralRulesCache[s];
          let a;
          try {
            a = new Intl.PluralRules(n, { type: o });
          } catch {
            if (!Intl) return (this.logger.error("No Intl support, please use an Intl polyfill!"), DOr);
            if (!e.match(/-|_/)) return DOr;
            let c = this.languageUtils.getLanguagePartFromCode(e);
            a = this.getRule(c, r);
          }
          return ((this.pluralRulesCache[s] = a), a);
        }
        needsPlural(e, r = {}) {
          let n = this.getRule(e, r);
          return (n || (n = this.getRule("dev", r)), n?.resolvedOptions().pluralCategories.length > 1);
        }
        getPluralFormsOfKey(e, r, n = {}) {
          return this.getSuffixes(e, n).map((o) => `${r}${o}`);
        }
        getSuffixes(e, r = {}) {
          let n = this.getRule(e, r);
          return (
            n || (n = this.getRule("dev", r)),
            n
              ? n
                  .resolvedOptions()
                  .pluralCategories.sort((o, s) => TOr[o] - TOr[s])
                  .map((o) => `${this.options.prepend}${r.ordinal ? `ordinal${this.options.prepend}` : ""}${o}`)
              : []
          );
        }
        getSuffix(e, r, n = {}) {
          let o = this.getRule(e, n);
          return o
            ? `${this.options.prepend}${n.ordinal ? `ordinal${this.options.prepend}` : ""}${o.select(r)}`
            : (this.logger.warn(`no plural rule found for: ${e}`), this.getSuffix("dev", r, n));
        }
      }),
      (IOr = (t, e, r, n = ".", o = !0) => {
        let s = Hgo(t, e, r);
        return (!s && o && ls(r) && ((s = P_t(t, r, n)), s === void 0 && (s = P_t(e, r, n))), s);
      }),
      (k_t = (t) => t.replace(/\$/g, "$$$$")),
      (COe = class {
        constructor(e = {}) {
          ((this.logger = mx.create("interpolator")),
            (this.options = e),
            (this.format = e?.interpolation?.format || ((r) => r)),
            this.init(e));
        }
        init(e = {}) {
          e.interpolation || (e.interpolation = { escapeValue: !0 });
          let {
            escape: r,
            escapeValue: n,
            useRawValueToEscape: o,
            prefix: s,
            prefixEscaped: a,
            suffix: u,
            suffixEscaped: c,
            formatSeparator: m,
            unescapeSuffix: d,
            unescapePrefix: f,
            nestingPrefix: p,
            nestingPrefixEscaped: h,
            nestingSuffix: g,
            nestingSuffixEscaped: b,
            nestingOptionsSeparator: A,
            maxReplaces: y,
            alwaysFormat: E,
          } = e.interpolation;
          ((this.escape = r !== void 0 ? r : Wgo),
            (this.escapeValue = n !== void 0 ? n : !0),
            (this.useRawValueToEscape = o !== void 0 ? o : !1),
            (this.prefix = s ? ote(s) : a || "{{"),
            (this.suffix = u ? ote(u) : c || "}}"),
            (this.formatSeparator = m || ","),
            (this.unescapePrefix = d ? "" : f || "-"),
            (this.unescapeSuffix = this.unescapePrefix ? "" : d || ""),
            (this.nestingPrefix = p ? ote(p) : h || ote("$t(")),
            (this.nestingSuffix = g ? ote(g) : b || ote(")")),
            (this.nestingOptionsSeparator = A || ","),
            (this.maxReplaces = y || 1e3),
            (this.alwaysFormat = E !== void 0 ? E : !1),
            this.resetRegExp());
        }
        reset() {
          this.options && this.init(this.options);
        }
        resetRegExp() {
          let e = (r, n) => (r?.source === n ? ((r.lastIndex = 0), r) : new RegExp(n, "g"));
          ((this.regexp = e(this.regexp, `${this.prefix}(.+?)${this.suffix}`)),
            (this.regexpUnescape = e(
              this.regexpUnescape,
              `${this.prefix}${this.unescapePrefix}(.+?)${this.unescapeSuffix}${this.suffix}`,
            )),
            (this.nestingRegexp = e(
              this.nestingRegexp,
              `${this.nestingPrefix}((?:[^()"']+|"[^"]*"|'[^']*'|\\((?:[^()]|"[^"]*"|'[^']*')*\\))*?)${this.nestingSuffix}`,
            )));
        }
        interpolate(e, r, n, o) {
          let s,
            a,
            u,
            c = (this.options && this.options.interpolation && this.options.interpolation.defaultVariables) || {},
            m = (h) => {
              if (h.indexOf(this.formatSeparator) < 0) {
                let y = IOr(r, c, h, this.options.keySeparator, this.options.ignoreJSONStructure);
                return this.alwaysFormat ? this.format(y, void 0, n, { ...o, ...r, interpolationkey: h }) : y;
              }
              let g = h.split(this.formatSeparator),
                b = g.shift().trim(),
                A = g.join(this.formatSeparator).trim();
              return this.format(IOr(r, c, b, this.options.keySeparator, this.options.ignoreJSONStructure), A, n, {
                ...o,
                ...r,
                interpolationkey: b,
              });
            };
          this.resetRegExp();
          let d = o?.missingInterpolationHandler || this.options.missingInterpolationHandler,
            f =
              o?.interpolation?.skipOnVariables !== void 0
                ? o.interpolation.skipOnVariables
                : this.options.interpolation.skipOnVariables;
          return (
            [
              { regex: this.regexpUnescape, safeValue: (h) => k_t(h) },
              { regex: this.regexp, safeValue: (h) => (this.escapeValue ? k_t(this.escape(h)) : k_t(h)) },
            ].forEach((h) => {
              for (u = 0; (s = h.regex.exec(e)); ) {
                let g = s[1].trim();
                if (((a = m(g)), a === void 0))
                  if (typeof d == "function") {
                    let A = d(e, s, o);
                    a = ls(A) ? A : "";
                  } else if (o && Object.prototype.hasOwnProperty.call(o, g)) a = "";
                  else if (f) {
                    a = s[0];
                    continue;
                  } else (this.logger.warn(`missed to pass in variable ${g} for interpolating ${e}`), (a = ""));
                else !ls(a) && !this.useRawValueToEscape && (a = vOr(a));
                let b = h.safeValue(a);
                if (
                  ((e = e.replace(s[0], b)),
                  f ? ((h.regex.lastIndex += a.length), (h.regex.lastIndex -= s[0].length)) : (h.regex.lastIndex = 0),
                  u++,
                  u >= this.maxReplaces)
                )
                  break;
              }
            }),
            e
          );
        }
        nest(e, r, n = {}) {
          let o,
            s,
            a,
            u = (c, m) => {
              let d = this.nestingOptionsSeparator;
              if (c.indexOf(d) < 0) return c;
              let f = c.split(new RegExp(`${d}[ ]*{`)),
                p = `{${f[1]}`;
              ((c = f[0]), (p = this.interpolate(p, a)));
              let h = p.match(/'/g),
                g = p.match(/"/g);
              (((h?.length ?? 0) % 2 === 0 && !g) || g.length % 2 !== 0) && (p = p.replace(/'/g, '"'));
              try {
                ((a = JSON.parse(p)), m && (a = { ...m, ...a }));
              } catch (b) {
                return (this.logger.warn(`failed parsing options string in nesting for key ${c}`, b), `${c}${d}${p}`);
              }
              return (a.defaultValue && a.defaultValue.indexOf(this.prefix) > -1 && delete a.defaultValue, c);
            };
          for (; (o = this.nestingRegexp.exec(e)); ) {
            let c = [];
            ((a = { ...n }),
              (a = a.replace && !ls(a.replace) ? a.replace : a),
              (a.applyPostProcessor = !1),
              delete a.defaultValue);
            let m = /{.*}/.test(o[1]) ? o[1].lastIndexOf("}") + 1 : o[1].indexOf(this.formatSeparator);
            if (
              (m !== -1 &&
                ((c = o[1]
                  .slice(m)
                  .split(this.formatSeparator)
                  .map((d) => d.trim())
                  .filter(Boolean)),
                (o[1] = o[1].slice(0, m))),
              (s = r(u.call(this, o[1].trim(), a), a)),
              s && o[0] === e && !ls(s))
            )
              return s;
            (ls(s) || (s = vOr(s)),
              s || (this.logger.warn(`missed to resolve ${o[1]} for nesting ${e}`), (s = "")),
              c.length &&
                (s = c.reduce((d, f) => this.format(d, f, n.lng, { ...n, interpolationkey: o[1].trim() }), s.trim())),
              (e = e.replace(o[0], s)),
              (this.regexp.lastIndex = 0));
          }
          return e;
        }
      }),
      (Zgo = (t) => {
        let e = t.toLowerCase().trim(),
          r = {};
        if (t.indexOf("(") > -1) {
          let n = t.split("(");
          e = n[0].toLowerCase().trim();
          let o = n[1].substring(0, n[1].length - 1);
          e === "currency" && o.indexOf(":") < 0
            ? r.currency || (r.currency = o.trim())
            : e === "relativetime" && o.indexOf(":") < 0
              ? r.range || (r.range = o.trim())
              : o.split(";").forEach((a) => {
                  if (a) {
                    let [u, ...c] = a.split(":"),
                      m = c
                        .join(":")
                        .trim()
                        .replace(/^'+|'+$/g, ""),
                      d = u.trim();
                    (r[d] || (r[d] = m),
                      m === "false" && (r[d] = !1),
                      m === "true" && (r[d] = !0),
                      isNaN(m) || (r[d] = parseInt(m, 10)));
                  }
                });
        }
        return { formatName: e, formatOptions: r };
      }),
      (ROr = (t) => {
        let e = {};
        return (r, n, o) => {
          let s = o;
          o &&
            o.interpolationkey &&
            o.formatParams &&
            o.formatParams[o.interpolationkey] &&
            o[o.interpolationkey] &&
            (s = { ...s, [o.interpolationkey]: void 0 });
          let a = n + JSON.stringify(s),
            u = e[a];
          return (u || ((u = t(R3e(n), o)), (e[a] = u)), u(r));
        };
      }),
      (ebo = (t) => (e, r, n) => t(R3e(r), n)(e)),
      (F_t = class {
        constructor(e = {}) {
          ((this.logger = mx.create("formatter")), (this.options = e), this.init(e));
        }
        init(e, r = { interpolation: {} }) {
          this.formatSeparator = r.interpolation.formatSeparator || ",";
          let n = r.cacheInBuiltFormats ? ROr : ebo;
          this.formats = {
            number: n((o, s) => {
              let a = new Intl.NumberFormat(o, { ...s });
              return (u) => a.format(u);
            }),
            currency: n((o, s) => {
              let a = new Intl.NumberFormat(o, { ...s, style: "currency" });
              return (u) => a.format(u);
            }),
            datetime: n((o, s) => {
              let a = new Intl.DateTimeFormat(o, { ...s });
              return (u) => a.format(u);
            }),
            relativetime: n((o, s) => {
              let a = new Intl.RelativeTimeFormat(o, { ...s });
              return (u) => a.format(u, s.range || "day");
            }),
            list: n((o, s) => {
              let a = new Intl.ListFormat(o, { ...s });
              return (u) => a.format(u);
            }),
          };
        }
        add(e, r) {
          this.formats[e.toLowerCase().trim()] = r;
        }
        addCached(e, r) {
          this.formats[e.toLowerCase().trim()] = ROr(r);
        }
        format(e, r, n, o = {}) {
          let s = r.split(this.formatSeparator);
          if (s.length > 1 && s[0].indexOf("(") > 1 && s[0].indexOf(")") < 0 && s.find((u) => u.indexOf(")") > -1)) {
            let u = s.findIndex((c) => c.indexOf(")") > -1);
            s[0] = [s[0], ...s.splice(1, u)].join(this.formatSeparator);
          }
          return s.reduce((u, c) => {
            let { formatName: m, formatOptions: d } = Zgo(c);
            if (this.formats[m]) {
              let f = u;
              try {
                let p = o?.formatParams?.[o.interpolationkey] || {},
                  h = p.locale || p.lng || o.locale || o.lng || n;
                f = this.formats[m](u, h, { ...d, ...o, ...p });
              } catch (p) {
                this.logger.warn(p);
              }
              return f;
            } else this.logger.warn(`there was no format function for ${m}`);
            return u;
          }, e);
        }
      }),
      (tbo = (t, e) => {
        t.pending[e] !== void 0 && (delete t.pending[e], t.pendingCount--);
      }),
      (U_t = class extends ste {
        constructor(e, r, n, o = {}) {
          (super(),
            (this.backend = e),
            (this.store = r),
            (this.services = n),
            (this.languageUtils = n.languageUtils),
            (this.options = o),
            (this.logger = mx.create("backendConnector")),
            (this.waitingReads = []),
            (this.maxParallelReads = o.maxParallelReads || 10),
            (this.readingCalls = 0),
            (this.maxRetries = o.maxRetries >= 0 ? o.maxRetries : 5),
            (this.retryTimeout = o.retryTimeout >= 1 ? o.retryTimeout : 350),
            (this.state = {}),
            (this.queue = []),
            this.backend?.init?.(n, o.backend, o));
        }
        queueLoad(e, r, n, o) {
          let s = {},
            a = {},
            u = {},
            c = {};
          return (
            e.forEach((m) => {
              let d = !0;
              (r.forEach((f) => {
                let p = `${m}|${f}`;
                !n.reload && this.store.hasResourceBundle(m, f)
                  ? (this.state[p] = 2)
                  : this.state[p] < 0 ||
                    (this.state[p] === 1
                      ? a[p] === void 0 && (a[p] = !0)
                      : ((this.state[p] = 1),
                        (d = !1),
                        a[p] === void 0 && (a[p] = !0),
                        s[p] === void 0 && (s[p] = !0),
                        c[f] === void 0 && (c[f] = !0)));
              }),
                d || (u[m] = !0));
            }),
            (Object.keys(s).length || Object.keys(a).length) &&
              this.queue.push({ pending: a, pendingCount: Object.keys(a).length, loaded: {}, errors: [], callback: o }),
            {
              toLoad: Object.keys(s),
              pending: Object.keys(a),
              toLoadLanguages: Object.keys(u),
              toLoadNamespaces: Object.keys(c),
            }
          );
        }
        loaded(e, r, n) {
          let o = e.split("|"),
            s = o[0],
            a = o[1];
          (r && this.emit("failedLoading", s, a, r),
            !r && n && this.store.addResourceBundle(s, a, n, void 0, void 0, { skipCopy: !0 }),
            (this.state[e] = r ? -1 : 2),
            r && n && (this.state[e] = 0));
          let u = {};
          (this.queue.forEach((c) => {
            (qgo(c.loaded, [s], a),
              tbo(c, e),
              r && c.errors.push(r),
              c.pendingCount === 0 &&
                !c.done &&
                (Object.keys(c.loaded).forEach((m) => {
                  u[m] || (u[m] = {});
                  let d = c.loaded[m];
                  d.length &&
                    d.forEach((f) => {
                      u[m][f] === void 0 && (u[m][f] = !0);
                    });
                }),
                (c.done = !0),
                c.errors.length ? c.callback(c.errors) : c.callback()));
          }),
            this.emit("loaded", u),
            (this.queue = this.queue.filter((c) => !c.done)));
        }
        read(e, r, n, o = 0, s = this.retryTimeout, a) {
          if (!e.length) return a(null, {});
          if (this.readingCalls >= this.maxParallelReads) {
            this.waitingReads.push({ lng: e, ns: r, fcName: n, tried: o, wait: s, callback: a });
            return;
          }
          this.readingCalls++;
          let u = (m, d) => {
              if ((this.readingCalls--, this.waitingReads.length > 0)) {
                let f = this.waitingReads.shift();
                this.read(f.lng, f.ns, f.fcName, f.tried, f.wait, f.callback);
              }
              if (m && d && o < this.maxRetries) {
                setTimeout(() => {
                  this.read.call(this, e, r, n, o + 1, s * 2, a);
                }, s);
                return;
              }
              a(m, d);
            },
            c = this.backend[n].bind(this.backend);
          if (c.length === 2) {
            try {
              let m = c(e, r);
              m && typeof m.then == "function" ? m.then((d) => u(null, d)).catch(u) : u(null, m);
            } catch (m) {
              u(m);
            }
            return;
          }
          return c(e, r, u);
        }
        prepareLoading(e, r, n = {}, o) {
          if (!this.backend)
            return (this.logger.warn("No backend was added via i18next.use. Will not load resources."), o && o());
          (ls(e) && (e = this.languageUtils.toResolveHierarchy(e)), ls(r) && (r = [r]));
          let s = this.queueLoad(e, r, n, o);
          if (!s.toLoad.length) return (s.pending.length || o(), null);
          s.toLoad.forEach((a) => {
            this.loadOne(a);
          });
        }
        load(e, r, n) {
          this.prepareLoading(e, r, {}, n);
        }
        reload(e, r, n) {
          this.prepareLoading(e, r, { reload: !0 }, n);
        }
        loadOne(e, r = "") {
          let n = e.split("|"),
            o = n[0],
            s = n[1];
          this.read(o, s, "read", void 0, void 0, (a, u) => {
            (a && this.logger.warn(`${r}loading namespace ${s} for language ${o} failed`, a),
              !a && u && this.logger.log(`${r}loaded namespace ${s} for language ${o}`, u),
              this.loaded(e, a, u));
          });
        }
        saveMissing(e, r, n, o, s, a = {}, u = () => {}) {
          if (this.services?.utils?.hasLoadedNamespace && !this.services?.utils?.hasLoadedNamespace(r)) {
            this.logger.warn(
              `did not save key "${n}" as the namespace "${r}" was not yet loaded`,
              "This means something IS WRONG in your setup. You access the t function before i18next.init / i18next.loadNamespace / i18next.changeLanguage was done. Wait for the callback or Promise to resolve before accessing it!!!",
            );
            return;
          }
          if (!(n == null || n === "")) {
            if (this.backend?.create) {
              let c = { ...a, isUpdate: s },
                m = this.backend.create.bind(this.backend);
              if (m.length < 6)
                try {
                  let d;
                  (m.length === 5 ? (d = m(e, r, n, o, c)) : (d = m(e, r, n, o)),
                    d && typeof d.then == "function" ? d.then((f) => u(null, f)).catch(u) : u(null, d));
                } catch (d) {
                  u(d);
                }
              else m(e, r, n, o, u, c);
            }
            !e || !e[0] || this.store.addResource(e[0], r, n, o);
          }
        }
      }),
      (O_t = () => ({
        debug: !1,
        initAsync: !0,
        ns: ["translation"],
        defaultNS: ["translation"],
        fallbackLng: ["dev"],
        fallbackNS: !1,
        supportedLngs: !1,
        nonExplicitSupportedLngs: !1,
        load: "all",
        preload: !1,
        simplifyPluralSuffix: !0,
        keySeparator: ".",
        nsSeparator: ":",
        pluralSeparator: "_",
        contextSeparator: "_",
        partialBundledLanguages: !1,
        saveMissing: !1,
        updateMissing: !1,
        saveMissingTo: "fallback",
        saveMissingPlurals: !0,
        missingKeyHandler: !1,
        missingInterpolationHandler: !1,
        postProcess: !1,
        postProcessPassResolved: !1,
        returnNull: !1,
        returnEmptyString: !0,
        returnObjects: !1,
        joinArrays: !1,
        returnedObjectHandler: !1,
        parseMissingKeyHandler: !1,
        appendNamespaceToMissingKey: !1,
        appendNamespaceToCIMode: !1,
        overloadTranslationOptionHandler: (t) => {
          let e = {};
          if (
            (typeof t[1] == "object" && (e = t[1]),
            ls(t[1]) && (e.defaultValue = t[1]),
            ls(t[2]) && (e.tDescription = t[2]),
            typeof t[2] == "object" || typeof t[3] == "object")
          ) {
            let r = t[3] || t[2];
            Object.keys(r).forEach((n) => {
              e[n] = r[n];
            });
          }
          return e;
        },
        interpolation: {
          escapeValue: !0,
          format: (t) => t,
          prefix: "{{",
          suffix: "}}",
          formatSeparator: ",",
          unescapePrefix: "-",
          nestingPrefix: "$t(",
          nestingSuffix: ")",
          nestingOptionsSeparator: ",",
          maxReplaces: 1e3,
          skipOnVariables: !0,
        },
        cacheInBuiltFormats: !0,
      })),
      (kOr = (t) => (
        ls(t.ns) && (t.ns = [t.ns]),
        ls(t.fallbackLng) && (t.fallbackLng = [t.fallbackLng]),
        ls(t.fallbackNS) && (t.fallbackNS = [t.fallbackNS]),
        t.supportedLngs?.indexOf?.("cimode") < 0 && (t.supportedLngs = t.supportedLngs.concat(["cimode"])),
        typeof t.initImmediate == "boolean" && (t.initAsync = t.initImmediate),
        t
      )),
      (AOe = () => {}),
      (rbo = (t) => {
        Object.getOwnPropertyNames(Object.getPrototypeOf(t)).forEach((r) => {
          typeof t[r] == "function" && (t[r] = t[r].bind(t));
        });
      }),
      ($_t = class t extends ste {
        constructor(e = {}, r) {
          if (
            (super(),
            (this.options = kOr(e)),
            (this.services = {}),
            (this.logger = mx),
            (this.modules = { external: [] }),
            rbo(this),
            r && !this.isInitialized && !e.isClone)
          ) {
            if (!this.options.initAsync) return (this.init(e, r), this);
            setTimeout(() => {
              this.init(e, r);
            }, 0);
          }
        }
        init(e = {}, r) {
          ((this.isInitializing = !0),
            typeof e == "function" && ((r = e), (e = {})),
            e.defaultNS == null &&
              e.ns &&
              (ls(e.ns) ? (e.defaultNS = e.ns) : e.ns.indexOf("translation") < 0 && (e.defaultNS = e.ns[0])));
          let n = O_t();
          ((this.options = { ...n, ...this.options, ...kOr(e) }),
            (this.options.interpolation = { ...n.interpolation, ...this.options.interpolation }),
            e.keySeparator !== void 0 && (this.options.userDefinedKeySeparator = e.keySeparator),
            e.nsSeparator !== void 0 && (this.options.userDefinedNsSeparator = e.nsSeparator),
            typeof this.options.overloadTranslationOptionHandler != "function" &&
              (this.options.overloadTranslationOptionHandler = n.overloadTranslationOptionHandler));
          let o = (m) => (m ? (typeof m == "function" ? new m() : m) : null);
          if (!this.options.isClone) {
            this.modules.logger ? mx.init(o(this.modules.logger), this.options) : mx.init(null, this.options);
            let m;
            this.modules.formatter ? (m = this.modules.formatter) : (m = F_t);
            let d = new vOe(this.options);
            this.store = new _Oe(this.options.resources, this.options);
            let f = this.services;
            ((f.logger = mx),
              (f.resourceStore = this.store),
              (f.languageUtils = d),
              (f.pluralResolver = new M_t(d, {
                prepend: this.options.pluralSeparator,
                simplifyPluralSuffix: this.options.simplifyPluralSuffix,
              })),
              this.options.interpolation.format &&
                this.options.interpolation.format !== n.interpolation.format &&
                this.logger.deprecate(
                  "init: you are still using the legacy format function, please use the new approach: https://www.i18next.com/translation-function/formatting",
                ),
              m &&
                (!this.options.interpolation.format || this.options.interpolation.format === n.interpolation.format) &&
                ((f.formatter = o(m)),
                f.formatter.init && f.formatter.init(f, this.options),
                (this.options.interpolation.format = f.formatter.format.bind(f.formatter))),
              (f.interpolator = new COe(this.options)),
              (f.utils = { hasLoadedNamespace: this.hasLoadedNamespace.bind(this) }),
              (f.backendConnector = new U_t(o(this.modules.backend), f.resourceStore, f, this.options)),
              f.backendConnector.on("*", (h, ...g) => {
                this.emit(h, ...g);
              }),
              this.modules.languageDetector &&
                ((f.languageDetector = o(this.modules.languageDetector)),
                f.languageDetector.init && f.languageDetector.init(f, this.options.detection, this.options)),
              this.modules.i18nFormat &&
                ((f.i18nFormat = o(this.modules.i18nFormat)), f.i18nFormat.init && f.i18nFormat.init(this)),
              (this.translator = new EOe(this.services, this.options)),
              this.translator.on("*", (h, ...g) => {
                this.emit(h, ...g);
              }),
              this.modules.external.forEach((h) => {
                h.init && h.init(this);
              }));
          }
          if (
            ((this.format = this.options.interpolation.format),
            r || (r = AOe),
            this.options.fallbackLng && !this.services.languageDetector && !this.options.lng)
          ) {
            let m = this.services.languageUtils.getFallbackCodes(this.options.fallbackLng);
            m.length > 0 && m[0] !== "dev" && (this.options.lng = m[0]);
          }
          (!this.services.languageDetector &&
            !this.options.lng &&
            this.logger.warn("init: no languageDetector is used and no lng is defined"),
            ["getResource", "hasResourceBundle", "getResourceBundle", "getDataByLanguage"].forEach((m) => {
              this[m] = (...d) => this.store[m](...d);
            }),
            ["addResource", "addResources", "addResourceBundle", "removeResourceBundle"].forEach((m) => {
              this[m] = (...d) => (this.store[m](...d), this);
            }));
          let u = D3e(),
            c = () => {
              let m = (d, f) => {
                ((this.isInitializing = !1),
                  this.isInitialized &&
                    !this.initializedStoreOnce &&
                    this.logger.warn("init: i18next is already initialized. You should call init just once!"),
                  (this.isInitialized = !0),
                  this.options.isClone || this.logger.log("initialized", this.options),
                  this.emit("initialized", this.options),
                  u.resolve(f),
                  r(d, f));
              };
              if (this.languages && !this.isInitialized) return m(null, this.t.bind(this));
              this.changeLanguage(this.options.lng, m);
            };
          return (this.options.resources || !this.options.initAsync ? c() : setTimeout(c, 0), u);
        }
        loadResources(e, r = AOe) {
          let n = r,
            o = ls(e) ? e : this.language;
          if ((typeof e == "function" && (n = e), !this.options.resources || this.options.partialBundledLanguages)) {
            if (o?.toLowerCase() === "cimode" && (!this.options.preload || this.options.preload.length === 0))
              return n();
            let s = [],
              a = (u) => {
                if (!u || u === "cimode") return;
                this.services.languageUtils.toResolveHierarchy(u).forEach((m) => {
                  m !== "cimode" && s.indexOf(m) < 0 && s.push(m);
                });
              };
            (o ? a(o) : this.services.languageUtils.getFallbackCodes(this.options.fallbackLng).forEach((c) => a(c)),
              this.options.preload?.forEach?.((u) => a(u)),
              this.services.backendConnector.load(s, this.options.ns, (u) => {
                (!u && !this.resolvedLanguage && this.language && this.setResolvedLanguage(this.language), n(u));
              }));
          } else n(null);
        }
        reloadResources(e, r, n) {
          let o = D3e();
          return (
            typeof e == "function" && ((n = e), (e = void 0)),
            typeof r == "function" && ((n = r), (r = void 0)),
            e || (e = this.languages),
            r || (r = this.options.ns),
            n || (n = AOe),
            this.services.backendConnector.reload(e, r, (s) => {
              (o.resolve(), n(s));
            }),
            o
          );
        }
        use(e) {
          if (!e)
            throw new Error(
              "You are passing an undefined module! Please check the object you are passing to i18next.use()",
            );
          if (!e.type)
            throw new Error("You are passing a wrong module! Please check the object you are passing to i18next.use()");
          return (
            e.type === "backend" && (this.modules.backend = e),
            (e.type === "logger" || (e.log && e.warn && e.error)) && (this.modules.logger = e),
            e.type === "languageDetector" && (this.modules.languageDetector = e),
            e.type === "i18nFormat" && (this.modules.i18nFormat = e),
            e.type === "postProcessor" && NOr.addPostProcessor(e),
            e.type === "formatter" && (this.modules.formatter = e),
            e.type === "3rdParty" && this.modules.external.push(e),
            this
          );
        }
        setResolvedLanguage(e) {
          if (!(!e || !this.languages) && !(["cimode", "dev"].indexOf(e) > -1)) {
            for (let r = 0; r < this.languages.length; r++) {
              let n = this.languages[r];
              if (!(["cimode", "dev"].indexOf(n) > -1) && this.store.hasLanguageSomeTranslations(n)) {
                this.resolvedLanguage = n;
                break;
              }
            }
            !this.resolvedLanguage &&
              this.languages.indexOf(e) < 0 &&
              this.store.hasLanguageSomeTranslations(e) &&
              ((this.resolvedLanguage = e), this.languages.unshift(e));
          }
        }
        changeLanguage(e, r) {
          this.isLanguageChangingTo = e;
          let n = D3e();
          this.emit("languageChanging", e);
          let o = (u) => {
              ((this.language = u),
                (this.languages = this.services.languageUtils.toResolveHierarchy(u)),
                (this.resolvedLanguage = void 0),
                this.setResolvedLanguage(u));
            },
            s = (u, c) => {
              (c
                ? this.isLanguageChangingTo === e &&
                  (o(c),
                  this.translator.changeLanguage(c),
                  (this.isLanguageChangingTo = void 0),
                  this.emit("languageChanged", c),
                  this.logger.log("languageChanged", c))
                : (this.isLanguageChangingTo = void 0),
                n.resolve((...m) => this.t(...m)),
                r && r(u, (...m) => this.t(...m)));
            },
            a = (u) => {
              !e && !u && this.services.languageDetector && (u = []);
              let c = ls(u) ? u : u && u[0],
                m = this.store.hasLanguageSomeTranslations(c)
                  ? c
                  : this.services.languageUtils.getBestMatchFromCodes(ls(u) ? [u] : u);
              (m &&
                (this.language || o(m),
                this.translator.language || this.translator.changeLanguage(m),
                this.services.languageDetector?.cacheUserLanguage?.(m)),
                this.loadResources(m, (d) => {
                  s(d, m);
                }));
            };
          return (
            !e && this.services.languageDetector && !this.services.languageDetector.async
              ? a(this.services.languageDetector.detect())
              : !e && this.services.languageDetector && this.services.languageDetector.async
                ? this.services.languageDetector.detect.length === 0
                  ? this.services.languageDetector.detect().then(a)
                  : this.services.languageDetector.detect(a)
                : a(e),
            n
          );
        }
        getFixedT(e, r, n) {
          let o = (s, a, ...u) => {
            let c;
            (typeof a != "object"
              ? (c = this.options.overloadTranslationOptionHandler([s, a].concat(u)))
              : (c = { ...a }),
              (c.lng = c.lng || o.lng),
              (c.lngs = c.lngs || o.lngs),
              (c.ns = c.ns || o.ns),
              c.keyPrefix !== "" && (c.keyPrefix = c.keyPrefix || n || o.keyPrefix));
            let m = this.options.keySeparator || ".",
              d;
            return (
              c.keyPrefix && Array.isArray(s)
                ? (d = s.map(
                    (f) => (
                      typeof f == "function" && (f = L_t(f, { ...this.options, ...a })),
                      `${c.keyPrefix}${m}${f}`
                    ),
                  ))
                : (typeof s == "function" && (s = L_t(s, { ...this.options, ...a })),
                  (d = c.keyPrefix ? `${c.keyPrefix}${m}${s}` : s)),
              this.t(d, c)
            );
          };
          return (ls(e) ? (o.lng = e) : (o.lngs = e), (o.ns = r), (o.keyPrefix = n), o);
        }
        t(...e) {
          return this.translator?.translate(...e);
        }
        exists(...e) {
          return this.translator?.exists(...e);
        }
        setDefaultNamespace(e) {
          this.options.defaultNS = e;
        }
        hasLoadedNamespace(e, r = {}) {
          if (!this.isInitialized)
            return (this.logger.warn("hasLoadedNamespace: i18next was not initialized", this.languages), !1);
          if (!this.languages || !this.languages.length)
            return (this.logger.warn("hasLoadedNamespace: i18n.languages were undefined or empty", this.languages), !1);
          let n = r.lng || this.resolvedLanguage || this.languages[0],
            o = this.options ? this.options.fallbackLng : !1,
            s = this.languages[this.languages.length - 1];
          if (n.toLowerCase() === "cimode") return !0;
          let a = (u, c) => {
            let m = this.services.backendConnector.state[`${u}|${c}`];
            return m === -1 || m === 0 || m === 2;
          };
          if (r.precheck) {
            let u = r.precheck(this, a);
            if (u !== void 0) return u;
          }
          return !!(
            this.hasResourceBundle(n, e) ||
            !this.services.backendConnector.backend ||
            (this.options.resources && !this.options.partialBundledLanguages) ||
            (a(n, e) && (!o || a(s, e)))
          );
        }
        loadNamespaces(e, r) {
          let n = D3e();
          return this.options.ns
            ? (ls(e) && (e = [e]),
              e.forEach((o) => {
                this.options.ns.indexOf(o) < 0 && this.options.ns.push(o);
              }),
              this.loadResources((o) => {
                (n.resolve(), r && r(o));
              }),
              n)
            : (r && r(), Promise.resolve());
        }
        loadLanguages(e, r) {
          let n = D3e();
          ls(e) && (e = [e]);
          let o = this.options.preload || [],
            s = e.filter((a) => o.indexOf(a) < 0 && this.services.languageUtils.isSupportedCode(a));
          return s.length
            ? ((this.options.preload = o.concat(s)),
              this.loadResources((a) => {
                (n.resolve(), r && r(a));
              }),
              n)
            : (r && r(), Promise.resolve());
        }
        dir(e) {
          if (
            (e || (e = this.resolvedLanguage || (this.languages?.length > 0 ? this.languages[0] : this.language)), !e)
          )
            return "rtl";
          try {
            let o = new Intl.Locale(e);
            if (o && o.getTextInfo) {
              let s = o.getTextInfo();
              if (s && s.direction) return s.direction;
            }
          } catch {}
          let r = [
              "ar",
              "shu",
              "sqr",
              "ssh",
              "xaa",
              "yhd",
              "yud",
              "aao",
              "abh",
              "abv",
              "acm",
              "acq",
              "acw",
              "acx",
              "acy",
              "adf",
              "ads",
              "aeb",
              "aec",
              "afb",
              "ajp",
              "apc",
              "apd",
              "arb",
              "arq",
              "ars",
              "ary",
              "arz",
              "auz",
              "avl",
              "ayh",
              "ayl",
              "ayn",
              "ayp",
              "bbz",
              "pga",
              "he",
              "iw",
              "ps",
              "pbt",
              "pbu",
              "pst",
              "prp",
              "prd",
              "ug",
              "ur",
              "ydd",
              "yds",
              "yih",
              "ji",
              "yi",
              "hbo",
              "men",
              "xmn",
              "fa",
              "jpr",
              "peo",
              "pes",
              "prs",
              "dv",
              "sam",
              "ckb",
            ],
            n = this.services?.languageUtils || new vOe(O_t());
          return e.toLowerCase().indexOf("-latn") > 1
            ? "ltr"
            : r.indexOf(n.getLanguagePartFromCode(e)) > -1 || e.toLowerCase().indexOf("-arab") > 1
              ? "rtl"
              : "ltr";
        }
        static createInstance(e = {}, r) {
          let n = new t(e, r);
          return ((n.createInstance = t.createInstance), n);
        }
        cloneInstance(e = {}, r = AOe) {
          let n = e.forkResourceStore;
          n && delete e.forkResourceStore;
          let o = { ...this.options, ...e, isClone: !0 },
            s = new t(o);
          if (
            ((e.debug !== void 0 || e.prefix !== void 0) && (s.logger = s.logger.clone(e)),
            ["store", "services", "language"].forEach((u) => {
              s[u] = this[u];
            }),
            (s.services = { ...this.services }),
            (s.services.utils = { hasLoadedNamespace: s.hasLoadedNamespace.bind(s) }),
            n)
          ) {
            let u = Object.keys(this.store.data).reduce(
              (c, m) => (
                (c[m] = { ...this.store.data[m] }),
                (c[m] = Object.keys(c[m]).reduce((d, f) => ((d[f] = { ...c[m][f] }), d), c[m])),
                c
              ),
              {},
            );
            ((s.store = new _Oe(u, o)), (s.services.resourceStore = s.store));
          }
          if (e.interpolation) {
            let c = { ...O_t().interpolation, ...this.options.interpolation, ...e.interpolation },
              m = { ...o, interpolation: c };
            s.services.interpolator = new COe(m);
          }
          return (
            (s.translator = new EOe(s.services, o)),
            s.translator.on("*", (u, ...c) => {
              s.emit(u, ...c);
            }),
            s.init(o, r),
            (s.translator.options = o),
            (s.translator.backendConnector.services.utils = { hasLoadedNamespace: s.hasLoadedNamespace.bind(s) }),
            s
          );
        }
        toJSON() {
          return {
            options: this.options,
            store: this.store,
            language: this.language,
            languages: this.languages,
            resolvedLanguage: this.resolvedLanguage,
          };
        }
      }),
      (I = $_t.createInstance()),
      (BSu = I.createInstance),
      (LSu = I.dir),
      (MSu = I.init),
      (FSu = I.loadResources),
      (USu = I.reloadResources),
      ($Su = I.use),
      (jSu = I.changeLanguage),
      (QSu = I.getFixedT),
      (GSu = I.t),
      (qSu = I.exists),
      (HSu = I.setDefaultNamespace),
      (VSu = I.hasLoadedNamespace),
      (WSu = I.loadNamespaces),
      (zSu = I.loadLanguages));
  });