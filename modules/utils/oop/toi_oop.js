/**
 * @module toi
 * @category utils/oop
 * @label oop
 * @position 1599 / 1990
 * @source esbuild bundle from @iflow-ai/iflow-cli v0.5.19
 * @package iflow-cli-ce (Community Edition)
 * @license Apache-2.0
 *
 * This module was automatically extracted and categorized.
 * The original variable name (toi) is preserved for compatibility.
 * DO NOT rename this module's variable - it's referenced by other modules.
 */


var toi = T((eoi, zle) => {
  "use strict";
  var Zii = [Dii(), kii(), Pii(), Lii(), Fii(), $ii(), Qii(), Hii(), Xii()];
  for ($st = 0; $st < Zii.length; $st++) {
    zle = Zii[$st];
    for (jst in zle) Object.prototype.hasOwnProperty.call(zle, jst) && (eoi[jst] = zle[jst]);
  }
  var zle, jst, $st;
});